{
    "required": [
        "refresh_token",
        "access_token"
    ],
    "additionalProperties": true,
    "properties": {
        "refresh_token": {
            "type": "string",
            "minLength": 1
        },
        "access_token": {
            "type": "string",
            "minLength": 1
        }
    }
}