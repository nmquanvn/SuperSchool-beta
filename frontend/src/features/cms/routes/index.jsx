//import RTL from '../layouts/RTL.jsx';
import Dashboard from '../layouts/Dashboard.jsx';

var indexRoutes = [
  { path: '/manager', name: 'Home', component: Dashboard, private: true },
  //{ path: '/rtl', name: 'RTL', component: RTL },
];

export default indexRoutes;
